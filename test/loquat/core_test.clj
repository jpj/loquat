(ns loquat.core-test
  (:use [clojure.test :refer :all]
        [loquat.core :refer :all]))


;; (deftest hello-world-test
;;   (is (= (index-handler (request :get "/"))
;;          {:status 200
;;           :body "Hello World"})))
